time = linspace(0, 100); % s
lat = zeros(length(time), 2);
lat(:, 1) = time;
lat(:, 2) = linspace(); % TODO
lon = zeros(length(time), 2);
lon(:, 1) = time;
lon(:, 2) = linspace(); % TODO
height = zeros(length(time), 2);
height(:, 1) = time;
height(:, 2) = linspace(); % TODO

lat_0 = ;
lon_0 = ;
angle_entry = ;
angle_exit = ;
amplitude = ;
width = ;
